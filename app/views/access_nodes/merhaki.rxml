@access_nodes.each do | access_node |
  xml.node {
    xml.mac(@access_node.mac)
    xml.ip(@access_node.ip)
    xml.name(@access_node.title)
    xml.lat(@access_node.lat)
    xml.lng(@access_node.lng)
    xml.is_active('true')
    xml.seconds_since_update('')
    xml.channel('')
    xml.build('Merhaki')
    xml.arch('mips')
#   @batman_kids.each do | batman_node |
      
#   end
    xml.users_in_last_day('Stuff')
    xml.usage_in_last_day_in_kbytes('')
  }
end